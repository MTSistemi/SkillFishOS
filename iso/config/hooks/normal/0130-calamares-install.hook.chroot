#!/bin/bash
set -e

# Install Calamares installer into the live image
# Config is placed via includes.chroot at /etc/calamares/

apt-get install -y --install-recommends calamares calamares-data 2>/dev/null ||     apt-get install -y calamares

# Create desktop launcher for Calamares
mkdir -p /usr/share/applications
cat > /usr/share/applications/calamares.desktop << 'DESKTOP'
[Desktop Entry]
Version=1.0
Type=Application
Name=Install SkillFishOS
GenericName=System Installer
Comment=Install SkillFishOS to disk
Exec=pkexec calamares
# ⚠️ NON `calamares`: quella e' la freccia viola del progetto a monte, ed era
# la prima icona che vedeva chi provava il sistema. L'immagine arriva con
# skillfish-theme, che sta nella lista 70.
Icon=skillfish-installer
Terminal=false
Categories=System;
Keywords=install;
DESKTOP

echo "Calamares installed."
