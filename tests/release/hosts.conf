# Test machines for tests/release/release-check.sh: kind, then ssh target.
# Both are reached as root with the build VM's key. Test machines only: the
# check switches hardware settings off and on, and puts each one back.
bc250  root@192.168.5.32
x64    root@192.168.5.22
